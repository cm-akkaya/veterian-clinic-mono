apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic-adminer
  namespace: ngn-springboot
  labels:
    app: petclinic-adminer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: petclinic-adminer
  template:
    metadata:
      labels:
        app: petclinic-adminer
    spec:
      containers:
        - name: adminer
          image: adminer:4.8.1
          ports:
            - containerPort: 8080
              name: http
          env:
            # Login formunda "Server" alanını otomatik dolduran ayar.
            # "postgres" burada k8s Service adı - pod DNS üzerinden çözüyor.
            - name: ADMINER_DEFAULT_SERVER
              value: "postgres"
            - name: ADMINER_DESIGN
              value: "pepa-linha"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: petclinic-adminer
  namespace: ngn-springboot
spec:
  selector:
    app: petclinic-adminer
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082   # apply etmeden once bu portun bos oldugunu dogrula
      name: http
