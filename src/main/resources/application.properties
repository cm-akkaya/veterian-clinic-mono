# Database
database=mysql

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/veterian?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER:veterian}
spring.datasource.password=${MYSQL_PASSWORD:veterian}

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
spring.sql.init.data-locations=classpath*:db/${database}/data.sql


# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategySnakeCaseImpl

spring.jpa.properties.hibernate.default_batch_fetch_size=16


# Web
spring.thymeleaf.mode=HTML


# Internationalization
spring.messages.basename=messages/messages


# Actuator
management.endpoints.web.exposure.include=*


# Logging
logging.level.org.springframework=INFO
logging.file.name=/app/logs/petclinic.log


# Static resources cache
spring.web.resources.cache.cachecontrol.max-age=12h